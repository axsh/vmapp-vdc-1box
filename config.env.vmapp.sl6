# vmbuilder variables

rootsize=$((1024 * 8))
raw=1box-${ASHIBA_ENV}.$(date +%Y%m%d).01.$(arch).raw

# kvm

mem_size=$((1024 * 4))
cpu_num=2

# mix jeos
[[ -f $(pwd)/jeos_profile.sh ]] && . $(pwd)/jeos_profile.sh || :
