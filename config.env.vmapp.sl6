# ashiba variables

declare vmapp_name=vmapp-1box

# vmbuilder variables

export rootsize=$((1024 * 8))
export raw=1box-${ASHIBA_ENV}.$(date +%Y%m%d).01.$(arch).raw

export gw=10.0.2.2
export ip=10.0.2.15
export mask=255.255.255.0
export hostname=wakame-vdc

# kvm

brname=vboxbr0
mem_size=$((1024 * 4))
cpu_num=2
